<?xml version="1.0" encoding="UTF-8"?> <!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ClientesKVV">

    <resultMap id="ResultClientesKVVID" type="Integer">
        <result property="value" column="id"/>
    </resultMap>

    <resultMap id="ResultClientesKVV" type="ClientesKVV">
        <result property="id" column="id"/>
        <result property="fecha_elaboracion" column="fecha_elaboracion"/>
        <result property="fecha_modificacion" column="fecha_modificacion"/>
        <result property="empresa_id" column="empresa_id"/>
        <result property="empresa" column="empresa"/>
        <result property="unidad_id" column="unidad_id"/>
        <result property="unidad" column="unidad"/>
        <result property="usuario_id" column="usuario_id"/>
        <result property="usuario" column="usuario"/>
        <result property="activo" column="activo"/>        
        <result property="razon_social" column="razon_social"/>
        <result property="nombre" column="nombre"/>
        <result property="domicilio" column="domicilio"/>
        <result property="ciudad" column="ciudad"/>
        <result property="estado" column="estado"/>
        <result property="rfc" column="rfc"/>
        <result property="cp" column="cp"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="tipo" column="tipo"/>
        <result property="permisos" column="permisos"/>

    </resultMap>

    <select id="ClientesKVVMaxID" resultMap="ResultClientesKVVID">
        SELECT max(id)+1 as id
        FROM clientes_kvv 
    </select>

    <insert id="ClientesKVVInsert" parameterType="ClientesKVV" useGeneratedKeys="true" keyProperty="id">

        INSERT INTO clientes_kvv 
        (fecha_elaboracion,fecha_modificacion,empresa_id,empresa,unidad_id,unidad,usuario_id,usuario,activo,razon_social,nombre,domicilio,ciudad,estado,rfc,cp,username,password,tipo,permisos)
        VALUES
        (#{fecha_elaboracion},#{fecha_modificacion},#{empresa_id},#{empresa},#{unidad_id},#{unidad},#{usuario_id},#{usuario},#{activo},#{razon_social},#{nombre},#{domicilio},#{ciudad},#{estado},#{rfc},#{cp},#{username},#{password},#{tipo},#{permisos})
       
        <selectKey keyProperty="id" resultType="Integer" order="AFTER" > 
              SELECT MAX(id) FROM clientes_kvv 
        </selectKey>

    </insert>

    <update id="ClientesKVVUpdate" parameterType="ClientesKVV">
        UPDATE clientes_kvv 
         SET fecha_elaboracion = #{fecha_elaboracion},fecha_modificacion = #{fecha_modificacion},empresa_id = #{empresa_id},empresa = #{empresa},unidad_id = #{unidad_id},unidad= #{unidad},
        usuario_id = #{usuario_id},usuario = #{usuario},activo = #{activo},razon_social = #{razon_social},nombre= #{nombre},domicilio = #{domicilio},ciudad = #{ciudad},estado = #{estado},rfc = #{rfc},cp = #{cp},username = #{username},password = #{password},tipo = #{tipo},permisos = #{permisos}
        WHERE id = #{id}
    </update>

    <delete id="ClientesKVVDelete" parameterType="map">
        DELETE FROM clientes_kvv 
        WHERE id = #{id}
    </delete>

    <select id="ClientesKVVWhere" parameterType="map" resultMap="ResultClientesKVV">
        SELECT *
        FROM clientes_kvv 
        ${where} 
        ${group} 
        ${order}
    </select>

    <select id="ClientesKVVOne" parameterType="map" resultMap="ResultClientesKVV">
        SELECT *
        FROM clientes_kvv 
        ${where} 
    </select>

</mapper>